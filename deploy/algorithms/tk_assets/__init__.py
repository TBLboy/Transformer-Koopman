"""Transformer-Koopman deployment assets (model + online lifter).

Historical note: this package was originally named ``aabb`` because the
upper-computer ``AlgorithmManager`` scans subdirectories alphabetically and
loads the controller before its dependencies — the original short name was
chosen to win that race. The functional name ``tk_assets`` (Transformer-
Koopman assets) is used after the rename; if you need alphabetical
ordering again, rename this folder before deployment.
"""
